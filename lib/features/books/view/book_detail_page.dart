import 'package:flutter/widgets.dart';

class BookDetailPage extends StatelessWidget {
  const BookDetailPage({super.key, required String bookId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}