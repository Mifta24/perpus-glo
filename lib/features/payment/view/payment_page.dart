import 'package:flutter/widgets.dart';

class PaymentPage extends StatelessWidget {
  const PaymentPage({super.key, required String fineId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}